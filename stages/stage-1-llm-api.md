# 阶段一 · LLM API 基本功

| | |
|---|---|
| **周次** | 第 1~2 周 |
| **预计工时** | 20 小时 |
| **前置** | 无。一个可用的 API Key + 你选的语言环境（Java 17+ / Go 1.22+） |
| **产出** | `projects/project-0-llm-gateway/`（建议 **Java 和 Go 各做一遍**，见轨道文档） |
| **一句话目标** | 像调用一个"不确定的 RPC 接口"一样调用大模型，并理解它的计费、限流和失败模式 |

> **语言实现**：本卡是语言无关的。具体用什么库、怎么写、会踩什么坑，见 [Java 轨道](../tracks/java.md) · [Go 轨道](../tracks/go.md)。双语分配策略见 [tracks/README.md](../tracks/README.md)。

> **心态提示**：这个阶段最反直觉的地方是——**同样的输入，输出可能不一样**。你过去五年的所有工程直觉都建立在确定性之上。先接受这一点，后面的评估、护栏、降级设计才讲得通。

---

## 任务清单

### T1.1 环境与第一个请求（2h）
- [ ] 引入官方 SDK（Java：`com.anthropic:anthropic-java`；Go：`github.com/anthropics/anthropic-sdk-go`）
- [ ] API Key 用环境变量 `ANTHROPIC_API_KEY`，**不要提交进仓库**（配 `.gitignore`，SDK 默认会读这个变量）
- [ ] 发出第一个请求，打印返回文本和 `usage`

**产出**：一个能跑的 main 方法。

### T1.2 多轮对话（3h）
- [ ] 理解：**服务端不存历史**，每轮都要把完整 `messages` 数组发回去
- [ ] 把 assistant 的回复也追加进历史（漏掉这步是新手第一号 bug）
- [ ] 历史存 Redis，按 sessionId 隔离，设 TTL
- [ ] 加历史长度上限（超了截断最早的几轮，或后面用压缩）

**产出**：`/chat` 接口，支持多轮。

### T1.3 流式输出（3h）
- [ ] 用 SDK 的 streaming API（`StreamResponse`）
- [ ] 用 SSE 暴露给客户端（Java：`SseEmitter` / WebFlux；Go：`http.Flusher`）
- [ ] 处理流中断：客户端断开时要关掉上游流，否则白烧 token
      （Go 靠 `ctx` 自动级联取消；Java 要手动注册回调——**这是两种语言差异最明显的地方之一，值得对照着写**）
- [ ] 测量首字延迟（TTFT）和总时长

**为什么必须做**：长输出用非流式容易撞 HTTP 超时；而且首字延迟从 8s 降到 0.8s，用户体验是两个产品。

### T1.4 结构化输出（3h）
- [ ] 用 structured outputs（`output_config.format`）或工具的 `strict: true` 拿到**保证合法**的 JSON
- [ ] 反序列化成强类型对象（Java：Jackson → DTO；Go：`encoding/json` → struct）
- [ ] 对比实验：用"请只返回 JSON"的 prompt 跑 50 次，统计失败率；再用结构化输出跑 50 次

**这是强类型语言接入 LLM 的关键点**：Java 和 Go 都最怕"有时候返回的不是 JSON"。别在 prompt 里跪求，用 API 能力解决。

### T1.5 Prompt Caching（3h）
- [ ] 理解渲染顺序：`tools` → `system` → `messages`，**前缀匹配**，前缀里一个字节变了后面全失效
- [ ] 把稳定内容（系统提示、工具定义、长知识片段）放前面并打缓存断点
- [ ] 把易变内容（时间戳、requestId、用户问题）放到断点之后
- [ ] 验证：看响应的 `usage.cache_read_input_tokens` 是否 > 0
- [ ] **做一次破坏性实验**：故意在 system prompt 里塞一个 `System.currentTimeMillis()`，观察命中率归零

最后这个实验必须亲手做一遍。这是线上缓存命中率突然掉到 0 的最常见原因，踩过一次就永远记得。

### T1.6 错误处理与重试（3h）
- [ ] 分清可重试（429 / 5xx / 网络超时）和不可重试（400 / 404）
- [ ] 按"最具体优先"写 catch 链：`NotFoundException` → `RateLimitException` → `AnthropicServiceException` → 连接异常。**不要一把 `catch (Exception)`**
- [ ] 429 读 `retry-after`，指数退避
- [ ] 注意：SDK 自带重试（默认 2 次），总耗时 = 超时 × (重试次数 + 1)，超时配置要按这个算

### T1.7 计量与成本（3h）
- [ ] 每次调用记录：input / output / cache read / cache write 四类 token
- [ ] 用 `count_tokens` 接口预估长输入的 token 数（**不要用字符数除以 N 估算，中文尤其不准**）
- [ ] 暴露 token 和耗时指标（Java：Micrometer；Go：`log/slog` + Prometheus client）
- [ ] 手算一次：一天 1 万次调用、平均输入 2000 token、输出 500 token，一个月多少钱

---

## 🎫 过关卡片

### 🎯 硬指标

| # | 指标 | 达标线 |
|---|---|---|
| 1 | 结构化输出稳定性 | 连续 50 次调用，反序列化失败 **0 次** |
| 2 | 缓存命中 | 相同前缀连续 20 次请求，`cache_read_input_tokens > 0` 的比例 **≥ 90%** |
| 3 | 流式首字延迟 | TTFT **< 2s**（P95） |
| 4 | 限流恢复 | 人为触发 429（或 mock），能自动退避并最终成功，日志可见重试轨迹 |
| 5 | 计量完整 | 任意一次调用能从日志还原出：四类 token 数 + 耗时 + 估算成本 |

### 🖥 演示题

现场演示 Project 0：发起一轮多轮对话，流式输出逐字出现，控制台同步打印 token 明细和本次成本。然后追问一个依赖上文的问题（"那它的缺点呢？"），证明历史管理是对的。

### 🗣 口试题（合上电脑作答）

1. 大模型 API 是无状态的，那"多轮对话"是怎么实现的？如果对话有 50 轮会发生什么？
2. 说出三个会让 prompt 缓存**静默失效**的写法。
3. output token 通常比 input token 贵 5 倍左右。这个事实会如何影响你的架构设计？（提示：至少能说出两点）
4. 什么情况下必须用流式？什么情况下流式反而是负担？
5. 为什么"在 prompt 里要求返回 JSON"不是一个工程解法？

### ✅ 通过标准

硬指标 **5/5**，演示题通过，口试题能答对 **4/5**。

---

## 常见卡点

| 症状 | 大概率原因 |
|---|---|
| 第二轮对话模型"失忆" | 忘了把上一轮 assistant 的回复追加进 `messages` |
| 缓存命中率一直是 0 | ① 前缀里有变量（时间、随机 id、Map 序列化顺序不固定）② 前缀 token 数没达到最小可缓存长度 ③ 断点打在了易变内容后面 |
| 长输出请求超时 | 没用流式。`max_tokens` 开大时必须流式 |
| token 估算和账单对不上 | 用字符数估算了。中文 token 密度和英文差异很大，用 `count_tokens` |
| 偶发 400 错误 | 检查是不是用了已被移除的参数（不同模型对参数的支持不同），或者 `max_tokens` 超过了模型上限 |
| 响应里文本是空的 | 先检查 `stop_reason`。可能是 `max_tokens` 截断，或者内容块里是思考块而不是文本块 |

> **提醒**：AI 领域 API 变化很快，网上教程（和 AI 给你的答案）里的参数写法经常已经过时。**遇到参数报错，第一反应是查官方文档当前版本**，而不是换个写法碰运气。
